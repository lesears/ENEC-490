%monte carlo
m = mean(totals)
s = std(totals);
%sample = m + (s*(randn(10)));

demandMean = m + s*rand(10,1);
demandSTD = m + s*rand(10,1);

for a = 1:10
    demand(a) = demandMean(a) + demandSTD(a)*randn(1);
end


figure
plot(demand)
